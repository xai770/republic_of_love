# Daily Notes — 2026-03-03 (Tue)

## Session start: 05:18 CET

## Plan: 14-point audit fix sweep

Yesterday's audit (commit pending from Mar 2) revealed 14 issues across
`search.py`, `search-app.js`, `search.html`, and `style.css`.
Fixing all of them today before committing the 6-tab search redesign.

### The 14 Points

| # | Sev | Area | Issue | Status |
|---|-----|------|-------|--------|
|  1 | CRIT | `search.py` | `SituationContextUpdate` model has wrong field names → 2/3 questionnaire saves return 422 | DONE |
|  2 | CRIT | `search.py` | `_score_ranked_results` fetches ALL matching rows — no LIMIT (OOM risk on 170K+ table) | DONE |
|  3 | CRIT | `search.py` | `_GEO_CACHE` plain dict with no thread safety; `clear()` at 2000 entries is racy | DONE |
|  4 | CRIT | `search.html` | `openFeedbackWidget()` referenced in onclick but never defined → JS error | DONE |
|  5 | CRIT | `search.html` | `{{ t('...') }}` inside `<script>` block is unescaped → XSS if i18n contains `'` or `</script>` | DONE |
|  6 | HIGH | `search-app.js` | Fire-and-forget tree loads (`loadSectorTree()` etc.) — no coordination | DONE |
|  7 | HIGH | `search-app.js` | Two separate `map.on('zoomend')` handlers — consolidate | DONE |
|  8 | HIGH | `search.py` | Profile embedding logic duplicated in `_score_ranked_results` AND `_attach_cosine_scores` | DONE |
|  9 | HIGH | `search.py` | Inconsistent geo filtering: preview uses haversine, intelligence uses ST_DWithin | DONE |
| 10 | HIGH | `search.py` | `SaveSearchRequest` only stores single lat/lon — loses multi-location + professions | DONE |
| 11 | MED  | `search-app.js` | Dead `renderIntelList()` function (empty stub) | DONE |
| 12 | MED  | `search.py` | `threading` import unused (now used by #3 fix) | DONE |
| 13 | MED  | `search-app.js` | `canvas.onmousemove` reassigned on every sparkline redraw — closure leak | DONE |
| 14 | MED  | `style.css` | Massive CSS duplication: `.spinner` 4×, `.hidden` 3×, `.btn-secondary` 4×, layout block 2× | DONE |

### Changes Made

**search.py:**
- `SituationContextUpdate` fields renamed: `confidence/openness/hours/environment` → `search_mode/field_change/intention`
- `_score_ranked_results` query now has `LIMIT 5000` cap
- `_GEO_CACHE` protected with `threading.Lock()`, eviction changed from `clear()` to drop-oldest-half
- New `_load_profile_embedding()` helper replaces 60+ duplicated lines in both `_attach_cosine_scores` and `_score_ranked_results`
- Intelligence endpoint geo filter standardized to use shared `_build_geo_where()` haversine helper
- `SaveSearchRequest` now accepts `states`, `professions`, `geo_locations` — save/load roundtrips fully

**search.html:**
- Removed dangling `openFeedbackWidget()` onclick
- All `{{ t('...') }}` in `<script>` blocks changed to `{{ t('...') | tojson }}` (XSS fix)
- Cache busters bumped: JS → `20260303a`, CSS → `20260303a`

**search-app.js:**
- Two `map.on('zoomend')` handlers consolidated into one
- `renderIntelList()` dead stub removed
- `canvas.onmousemove` → single persistent `addEventListener` with `._sparklinePoints` reference
- Tree loads wrapped in `Promise.allSettled()` for coordination
- Save/load roundtrip now includes states, professions, geo_locations

**style.css:**
- 296 lines removed (8013 → 7717)
- Eliminated: 3 extra `.spinner + @keyframes`, 2 extra `.hidden`, 3 extra `.btn-secondary`, duplicate layout block (~76 lines of header/sidebar/nav/hamburger)

---
