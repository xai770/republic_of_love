# 2026-02-25 — Morning Session

Coffee on. Second day.

---

## State of Play

Arrived from yesterday's cleanup session. Everything committed, repo clean.
Onboarding wizard is shipped. `user_trial_balance` renamed to `user_usage_summary`.
Actor smoke tests cover all 18 actors.

### What's live
- Full onboarding wizard, 12 steps, 3 locales (EN / DE-du / DE-Sie)
- Tier names: Guest / Member / Sustainer (Gast / Mitglied / Unterstützer)
- Journey board on `/dashboard` (Resume → Search → Apply)
- Match pipeline, embeddings, Mira, profile system

---

## Today: `/dashboard` → `/home` + Activity Log + Yog-meter

### Rename `/dashboard` → `/home`

The API was already half-renamed: `/api/home/stats` and `/api/home/journey` exist.
The UI routes are still `/dashboard` (and a `/dashboard-legacy` stub).
Sidebar, all templates, auth redirects reference `/dashboard`.

Plan:
- `GET /home` route (plus redirect `/dashboard` → `/home` for old links)
- Rename `frontend/templates/dashboard.html` → `home.html`
- Update sidebar, base template, all back-links, auth post-login redirect

### Activity Log

Table already exists: `yogi_posting_events` (migration `yogi_posting_events_20260223.sql`).
Event types: `viewed | saved | dismissed | apply_intent | applied | not_applied | outcome_received`
First-person narrative format: "I reviewed [title] → I decided to apply/not apply because..."
- Show last N entries on home, expand for full history
- Download as plain text (no editing on site)
- No PII in the log itself — posting title/URL joined at render time

### Yog-meter (funnel chart)

Already named "Yog-meter" in the migration comments.
Funnel stages from `yogi_posting_events` event_type counts:
  viewed → saved → dismissed / apply_intent → applied → outcome_received

Data model is ready. Need:
- API endpoint: `GET /api/home/yogmeter?profile_id=X`
- Frontend: horizontal waterfall/funnel bar chart
- Placement: home page, below journey board

---

## Fixes / Commits

### commit `da66379` — feat: /home page + Susanne + Activity Log + Yogi-meter

**Migration 063** — `users.is_protected BOOLEAN NOT NULL DEFAULT FALSE`  
xai (user_id=1) immediately set to `is_protected = TRUE`.

**Susanne Mustermann** (`scripts/seed_susanne.py`) — idempotent demo yogi:
- user_id=8, profile_id=41, email=`susanne.demo@talent.yoga.local`
- Kauffrau für Büromanagement, Berlin, 5 years, made redundant
- 28 real postings matched from live DB (Büromanagement/Assistenz/Verwaltung)
- 54 events seeded over 90 days: 25 viewed, 9 saved, 7 dismissed, 6 apply_intent,
  4 applied, 2 not_applied, 1 outcome_received
- Login: `http://localhost:8000/auth/test-login/8`

**`/dashboard` → `/home`** — full rename:
- `GET /home` is the page; `GET /dashboard` returns 301 → `/home`
- Updated: `api/main.py`, `api/routers/auth.py` (both OAuth + debug login),
  `api/routers/subscription.py` (Stripe return_url),
  `frontend/templates/partials/sidebar.html`,
  `frontend/templates/admin/console.html`,
  `frontend/templates/dashboard.html` (active_page + tour pathname check),
  all inline links in `api/routers/dashboard.py`

**`GET /api/home/activity`** — returns last N yogi events as first-person German
sentences (rule-based, no LLM). Each row: `sentence`, `event_type`, `created_at`.

**`GET /api/home/yogimeter`** — returns `stages[]` (count per event_type),
`total_matches`, `days_active`.

**`dashboard.html`** — two new panels below the journey board:
- **Mein Verlauf** (Activity Log): colour-coded per event_type, date shown
- **Yogi-Meter**: horizontal bar funnel, CSS-only animation, lazy JS load
### commit `TBD` — feat: Bewerbungsprotokoll panel

Third home-page component. Full job-search log for employment agency compliance.
Discussed explicitly in Feb 23 note: "Nachweisbar für die Agentur für Arbeit."

**`GET /api/home/protokoll`** — full event log as JSON.
- Joins `postings` for job_title/city/URL; joins `profile_posting_matches` for
  go_reasons/nogo_reasons/recommendation/user_decision
- `?decisions_only=true` (default) shows only meaningful decisions
  (dismissed, apply_intent, applied, not_applied, outcome_received)
- Returns `total_applications`, `total_decisions` for the footer summary

**`GET /api/home/protokoll/download`** — plain-text `.txt` download.
- Chronological order (oldest first), grouped by month
- Header: name, location, date, "Erstellt von talent.yoga"
- Each entry: date/time, job title + city + URL, action, Begründung
- Footer: legal statement that the log can be submitted to Agentur für Arbeit
- Returns as `application/text; attachment; filename=bewerbungsprotokoll_YYYYMMDD.txt`

**UI panel** — full-width below Activity Log + Yogi-meter:
- Green badge: "Nachweisbar für die Agentur für Arbeit"
- Toggle button: "Nur Entscheidungen" (filters to decisions, on by default)
- Download button → hits the text export endpoint directly
- Table: Datum | Stelle (clickable if external_url) | Aktion (colour chip) | Begründung
- Shows first 20 rows; "Alle N Einträge anzeigen" loads rest client-side (no second request)
