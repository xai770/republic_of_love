# 2026-02-24 — Morning Session

Good morning. Coffee on. Let's go.

---

## State of Play

Yesterday landed eight commits — the biggest of which was the dual-language profile
architecture (`6120eac`). Today we build on that and keep momentum.

### What's live as of this morning
- Immutable audit log + freeze controls (`51e7694`)
- GDPR consent gate on CV upload (`71ee189`, `a1c3672`, `0ea5ae4`, `33c191c`)
- Profile-aware Adele greeting (`4265bfe`)
- `profiles.language` + non-destructive translation cache (`6120eac`)
- Clara routes embeddings by posting language — DE profile vs EN job no longer penalised
- Profile form split into **Experience** | **Skills** inner tabs with translation banner

---

## Today's Agenda

1. **Fix false-positive duplicate warning in `pipeline_health.py`** ← doing now  
   Query mirrors the partial unique index condition (invalidated=false AND enabled=true).

2. **Backfill `profiles.language`** for existing users via one-off script  
   (`langdetect` on `profile_summary` for rows where language is still NULL / 'de' default)

3. **Test translation banner** end-to-end with a real account

4. **`/matches` Kanban view** — carry forward from yesterday's roadmap discussion

---

## Fixes / Commits

<!-- appended as the day goes on -->

### 19:35 — Onboarding wizard polish session

Long afternoon/evening session iterating on the onboarding wizard. The meaningful stuff:

- **Variable card sizes per step** (`f83729c`) — wizard no longer one-size-fits-all.
  Each step morphs to its own width (540–768px) during the fade transition.
  Compact steps (manual name entry, language settings, notifications) breathe;
  content-heavy steps (welcome letter, data sources) keep room.

- **Du/Sie hover preview on step 3** (`726d858`) — hovering a formality card
  switches the title/subtitle live, same pattern as the flag hover on step 1.
  "Wie wollen wir uns anreden?" ↔ "Wie möchten Sie angesprochen werden?"

- **Step counter now covers all steps** (`dbb08f2`) — was 1–8 only, now 1–11
  (step 9 CV included). EN correctly maps around the skipped Du/Sie step.

- **Step 9 (CV upload) no longer a dead end** (`dbb08f2`) — added ← Zurück button,
  skip changed from long underlined link to clean ghost button (`eaeafdd`).

- **Step 10 (language settings) cleanup** (`3e4a050`) — added ← Zurück,
  flags 🇩🇪/🇬🇧 in dropdown options, subtitle simplified to
  "Bitte passen Sie diese Einstellungen bei Bedarf an."

- **Step 3 widened to 700px** (`8a6ae28`) — "Wie möchten Sie angesprochen werden?"
  was wrapping to two lines and causing the card to jump height on hover.

All cosmetic, but the wizard feels noticeably more alive now.

---

### 21:58 — Evening: step 11 redesign + bug fixes

Finished the day with step 11 (notification opt-in) and a run of small polish fixes.

- **Step 11 fully redesigned** (`db05fc0`) — replaced the old title/subtitle/two-button
  layout with a proper four-paragraph body explaining the privacy model, then a single
  dynamic continue button that reads:
  - "Weiter, ich möchte nicht benachrichtigt werden" (no email entered)
  - "Weiter, ich möchte benachrichtigt werden" (valid email typed)
  Label switches live on `oninput` via a simple regex email check.
  All three COPY variants (en, de_du, de_sie) rewritten.
  Card widened from 540px → 660px to fit the longer text.
  Also fixed a pre-existing dead bug: notification JS was referencing `#s10-*` IDs
  instead of `#s11-*`, so the feature had never actually worked.

- **`brandify` scope fix** (`ce128fc`) — step 11 body rendering called `brandify()`
  from inside `applyText()`, but it was only defined as a `const` inside two nested
  `if` blocks (step 2 render and `previewLang`). ReferenceError → entire `goTo()`
  chain died mid-fade, freezing the card. Fixed by hoisting `brandify` to IIFE level.
  This also broke the flag buttons on step 1 — both fixed in one commit.

- **Stuck spinner on back-from-step-9** (`94714b2`) — second instance of the confirm
  button getting stuck. On success, `completeOnboarding()` called `goTo(9)` but never
  restored the button it had just disabled+spun. Coming back from step 9 to step 8
  found it still frozen. Two-part fix: restore button before `goTo(9)` on success path;
  call `_resetConfirmButtons()` in `goTo()` whenever the target is step 8.

- **Step 9 skip button** (`30db5ae`) — `btn-ghost` → `btn-secondary` so it matches
  the ← Zurück button visually. No more underlined orphan.

- **Step 10 settings note above buttons** (`69fef07`) — "These can be changed anytime
  in Settings." moved from below the button row to between the dropdowns and buttons.

- **Step 11 email copy** (`b223261`) — last paragraph softened to mention the setting
  is changeable anytime in Settings, removing the slight pressure of the original phrasing.
---

### 2026-02-24 — Tier rename: Guest / Member / Sustainer

Decision: the three participation tiers are now called **Guest**, **Member**, and **Sustainer**.

- **Guest** (was: Free) — warm and honest; implies a natural social progression to membership
- **Member** (was: Standard) — signals belonging and contribution, not a product grade
- **Sustainer** — unchanged

German: **Gast** / **Mitglied** / **Unterstützer** (Unterstützer unchanged)

Naming alternatives considered: Standard (too mediocre), Pro (SaaS vocabulary), Free Member / Full Member (redundant), Free / Member / Sustainer (still cold). Guest / Member / Sustainer chosen.

Updated across:
- `docs/project/how_ty_works.md` — EN + DE tier headings, prose, feature table
- `frontend/templates/onboarding.html` — s12 plan labels (EN/DE) + LEGAL body (EN/DE)- `docs/project/billing_methodology.md` — Trial tier removed, Free→Guest, Full→Member, €9.90→€5.00

---

### 2026-02-25 — Onboarding wizard: complete ✅

Commits: `e220615` → `8c58367` → `ca1cd9c` → `9dc74ff` → `94ef8de`

The onboarding wizard is done. All 12 steps are live across three locales (EN / DE-du / DE-Sie).

Final piece was step 12 — the celebration / welcome float — which went through three rounds:

1. **Redesigned to Member-on-trial** (`ca1cd9c`) — "Welcome to the team!" heading,
   €5 credits grant copy, green "🌿 Member" badge pill, amber "on trial" tag, trial
   expiry note, CTA → home. Full COPY in all three locales with formal/informal DE split.

2. **Tier comparison table** (`9dc74ff`) — added an 8-row Guest/Member/Sustainer table
   directly on the welcome float. Users see what they're entering at the same moment they're
   told they've arrived. HTML injected via `innerHTML` from COPY locale strings.

3. **Spacing tightened** (`94ef8de`) — button was sitting on the lower border edge.
   Seven CSS property adjustments: padding, emoji margin, title margin, credit-intro gap,
   tier-row gap, trial-note gap, table bottom margin.

Result confirmed in DE-Sie locale screenshot. Clean hierarchy, readable badge, honest
"auf Probe" framing, full tier context — without feeling like a sales pitch.

The onboarding is shipped.
The onboarding is shipped.

---

## Coffee break debrief — things that stink

A short honest audit of what deserves attention before the next feature push.

### Task A — Rename `user_trial_balance` → `user_usage_summary`
The view does far more than track trial balance: subscription status, unbilled cents,
`needs_payment` flag, event count, last event. The name is a lie waiting to mislead
someone at 11pm. Fix: one migration with `DROP VIEW / CREATE VIEW` + schema.sql update
+ grep any callers.
**Status:** [ ] pending

### Task B — Clean up `temp/`
Four JSON files: `conversation_gemma3_4b.json`, `conversation_qwen2_5_7b.json`,
`task_type_llama3_1_latest.json`, `task_type_qwen2_5_7b.json`. Probably experiment
artefacts. Confirm throwaway, delete, done.
**Status:** [ ] pending

### Task C — Finish step 5 HTML rewrite
Marked `[-]` on the todo. Current HTML is minimal (title + one read-button + nav).
Was earmarked for a content/visual upgrade. Needs a look to decide: done enough, or
actually needs work?
**Status:** [ ] pending

### Task D — Actor pre-flight test coverage
`pipeline_health.py` handles runtime monitoring. No pre-flight smoke tests exist for
the actors directory (14 actors, all touching DB + AI pipeline). A silent bad actor
corrupting embeddings could affect every match result undetected for weeks.
Proposed: `tests/test_actors_smoke.py` — dry-run each actor with a fixture record,
assert on exit code + no DB errors.
**Status:** [ ] pending

### Task E — Commit everything + push
Unstaged: daily note, memo_billing_model_for_sandy_20260221.md,
sandy_20260221_week_review.md, deleted docs (offerings.md, our_commitment.md),
sql/SELECT..sql. Untracked: backups, nate note, schema.sql, profil.md,
ty_yogi_name_guidelines.md. Stage the right ones, skip backups + cruft.
**Status:** [ ] pending

