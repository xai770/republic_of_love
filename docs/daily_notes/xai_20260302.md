# 2026-03-02 — Search UX Polish: Contrast, Maximize, Filter Toolbar

Early start (04:22). Continuing from March 1 (12 commits, all 5 feedbacks closed).

---

## Commit `f68fbad` — search: city-pill contrast fix, maximizable panels, filter toolbar

### 1. City-pill dark mode contrast fix

**Problem:** "Hessen › Frankfurt am Main" pill nearly invisible — bright font on
bright background in dark mode.

**Root cause:** A pre-existing `.city-pill` rule at line ~5499 used `!important`
on `background: #e0f2fe` with no dark-mode override. This overrode our new
`[data-theme="dark"] .filter-pill.city-pill` rules.

**Fix:**
- Removed the old `.city-pill { background: #e0f2fe !important }` rule
- Added explicit `.filter-pill.city-pill` colors for both themes:
  - Light: `#e8f0fe` bg, `#1a56db` text
  - Dark: `#1a2744` bg, `#93bbfc` text (~7:1 contrast ratio)

### 2. Maximizable panels

Added ⛶ maximize button to all 3 search panels (left, map, right) + results
grid below. Click toggles full-width view; click ⊗ to restore.

**Implementation:**
- `.panel-maximize-btn` — absolute positioned, opacity 0→1 on panel hover
- `.search-panels.has-maximized` — switches grid to `1fr` single column
- Non-maximized siblings get `display: none !important` (needed because
  `.panel-map { display: flex }` at line 5505 wins by cascade otherwise)
- Results grid maximizes as fixed overlay (`z-index: 900`)
- Calls `map.invalidateSize()` after toggle for proper Leaflet rendering

### 3. Filter management toolbar

Replaced single "Save search" button with 4-button toolbar:

| Button | Action |
|--------|--------|
| 👤 From profile | Clears filters, re-infers from profile scope |
| 🗑 Clear all | Empties all state arrays, resets map to Germany center |
| 📂 Load saved | Loads previously saved search from server |
| 💾 Save search | Persists current filters to server (unchanged logic) |

Added i18n keys for all new buttons in both `en.json` and `de.json`.

### Files changed (4 files, +297 −8)

- `frontend/static/css/style.css` — city-pill colors, maximize button/panel CSS, toolbar button styles
- `frontend/templates/search.html` — maximize buttons HTML, toolbar HTML, JS handlers
- `frontend/static/i18n/en.json` — 9 new keys
- `frontend/static/i18n/de.json` — 9 new keys

---

## Testing

- Fresh page load: 0 console errors, 1 warning
- Maximize left panel: map + right panel hidden correctly (verified computed `display: none`)
- Clear All: 174,887 unfiltered results, all pills removed
- Reload from Profile: filters restored (18,120 results)
- Load Saved: restored saved search from server
- City-pill dark mode: `#93bbfc` text on `#1a2744` bg — clearly readable
- City-pill light mode: `#1a56db` text on `#e8f0fe` bg — good contrast

---

## Uncommitted — 6-tab guided search redesign (9 files, +1813 −202)

All work below is in the working tree, not yet committed.

### 1. DB + API: situation_context column + endpoints

- Added `situation_context JSONB` column to `user_profiles` (schema.sql)
- 3 new endpoints in `api/routers/search.py`:
  - `GET /api/search/situation` — load saved answers
  - `POST /api/search/situation` — save a single answer
  - `DELETE /api/search/situation` — clear all answers

### 2. HTML: 6-tab template (search.html, +343 lines)

Replaced the 3-panel power layout with a 6-tab structure:

| Tab | Content |
|-----|---------|
| 🧭 Starting Point | 3 situation questions (search_mode, field_change, intention) |
| 💼 Direction | Sector tile grid with 2-level zoom |
| ⭐ Level | Full-width QL strip + demand breakdown |
| 📍 Location | Map + state/city tree (same as before) |
| 🎯 Opportunities | Results grid + sparkline + intelligence |
| ⚡ Power Search | Dense 3-panel layout (sector tree, map, location tree) |

### 3. CSS: tab layouts + tile grid (style.css, +647 lines)

- Tab bar styling (`.search-tab`, `.tab-content`, active states)
- Situation cards (`.sq-card`, `.sq-question-sub`, expand/collapse, dark mode)
- Direction tiles (`.direction-tiles` 3-column grid, `.direction-tile` cards
  with `--dt-color` CSS variable for sector colors, proportional bars,
  profession tiles, sector header tile, breadcrumb, fade animation)
- Dark mode for all new components
- Responsive: 3→2→1 columns on mobile

### 4. JS: tab switching + dual-render + tiles (search-app.js, +763 lines)

Key additions:
- **Tab switching** — `switchTab()` with Leaflet invalidateSize guard when
  map becomes visible. State persisted via localStorage.
- **Situation questionnaire** — `SQ_QUESTIONS = ['search_mode', 'field_change', 'intention']`.
  Cards render from i18n keys, answers saved to `/api/search/situation`.
- **Dual-render** — QL strip, sector tree, location tree, total, sparkline,
  and results all render to both their primary tab AND the Power Search tab.
- **Power tab controls** — City search + radius select with bidirectional sync.
- **Direction tile grid** — `renderDirectionTiles()` with 2 zoom levels:
  - Level 1: 17 sector tiles sorted by count, color-coded borders,
    position counts, fresh badges, proportional bars, selected/dimmed states
  - Level 2: Click sector → zoom to profession grid. Breadcrumb for back.
    Click profession → toggles filter pill. Sector header tile for "Select all".
  - Search box filters within current zoom level.
- **Heatmap crash fix** — `renderHeatmap()` guard when `offsetWidth === 0`.
  All `map.invalidateSize()` calls wrapped with `offsetWidth > 0` check.
  Individual try/catch for map renders in `_doSearchNow()`.

### 5. i18n: revised wording (en.json + de.json, +47 each)

- Tab labels: "Starting Point" / "Ausgangslage"
- 3 question groups with 5+5+3 answer options
- Direction tile keys: `direction_all_sectors`, `direction_back`,
  `direction_select_all`, `direction_positions`, `direction_new_this_week`
- Question wording from Gershon+Nate spec, Arden parked Q3 (work environment)
  and trimmed email reference from intro

### 6. Bug fixes

- **Heatmap crash in hidden tab** (critical): `leaflet-heat.js` threw
  `IndexSizeError` when map canvas was 0-width. Single `catch` in
  `_doSearchNow()` killed the entire pipeline — sectors, locations, results
  never loaded. Fixed with offsetWidth guard + individual try/catch.
- **map.invalidateSize() on hidden tabs**: All 7 call sites now guarded
  with `offsetWidth > 0` to prevent leaflet-heat from crashing when
  heatmap layer exists but map is in a hidden tab.

### Testing — 6-tab full walkthrough

| Tab | Status | Notes |
|-----|--------|-------|
| Starting Point | ✅ | 3 questions rendered, "Intention" persisted, Adjust works |
| Direction | ✅ | 17 sector tiles, zoom into professions, breadcrumb back, profession toggle adds pill |
| Level | ✅ | 4 QL buttons with counts, selected state matches pills |
| Location | ✅ | Map + 16 Bundesländer, 6,631 positions, heatmap renders |
| Opportunities | ✅ | Results, sparkline, intelligence panel |
| Power Search | ✅ | Dense tree view, sector tree with expand/collapse, all data |
| Console errors | ✅ | 0 errors after full tab cycle |
| Tab persistence | ✅ | Reload restores last active tab from localStorage |

Cache busters: CSS `20260302d`, JS `20260302i`
